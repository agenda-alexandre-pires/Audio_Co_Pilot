#pragma once
#include <JuceHeader.h>

namespace Theme {
    const juce::Colour background = juce::Colour::fromString ("ff08090a"); // Fundo Profundo
    const juce::Colour surface    = juce::Colour::fromString ("ff1c2128"); // Cards (Grafite)
    const juce::Colour accent     = juce::Colour::fromString ("ff0080ff"); // Azul Neon
    const juce::Colour textMain   = juce::Colour::fromString ("ffffffff"); // Branco
    const juce::Colour textDim    = juce::Colour::fromString ("ff8e959e"); // Legendas
    const juce::Colour grid       = juce::Colour::fromString ("ff2d333b"); // Réguas e Grades
    const float cornerRadius = 12.0f;
}
